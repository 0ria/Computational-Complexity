#include "Transition.h"
#include "State.h"
#include "vector"

class FSA
{
private:
  // Vector con los estados
  // Vector con las transiciones
public:
  FSA(/* args */);
  FSA(std::vector<std::string>);
  ~FSA();
};